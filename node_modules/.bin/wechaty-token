#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../wechaty-puppet-service/node_modules/wechaty-token/dist/esm/bin/cli.js" "$@"
  ret=$?
else 
  node  "$basedir/../wechaty-puppet-service/node_modules/wechaty-token/dist/esm/bin/cli.js" "$@"
  ret=$?
fi
exit $ret
